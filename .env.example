# Dev.to API Key - Get from https://dev.to/settings/extensions
DEV_TO_API_KEY=your_dev_to_api_key

# AWS Configuration
# Run 'aws configure' to set up credentials (recommended)
# Or set AWS_PROFILE if using named profiles
# AWS_PROFILE=default
AWS_REGION=us-east-1

# Optional: Image hosting service API key (if using external hosting)
# IMGBB_API_KEY=your_imgbb_api_key
